# TOOLS.md

CLI tool inventory for this machine.

| Tool | Description | Help Command | How To Update |
| --- | --- | --- | --- |
| `bat` | `cat` replacement with syntax highlighting and paging. | `bat --help` | Download the latest official release binary for macOS arm64 and replace the local `bat` binary in `~/bin`. |
| `btm` | Terminal resource monitor for CPU, memory, disks, and processes. | `btm --help` | Download the latest official release binary for macOS arm64 and replace the local `btm` binary in `~/bin`. |
| `chezmoi` | Dotfiles manager for syncing and templating local configuration. | `chezmoi --help` | Download the latest official release binary for macOS arm64 and replace the local `chezmoi` binary in `~/bin`. |
| `delta` | Syntax-highlighted pager for `git diff`, `git show`, and patch output. | `delta --help` | Download the latest official release binary for macOS arm64 and replace the local `delta` binary in `~/bin`. |
| `fd` | Fast, user-friendly alternative to `find`. | `fd --help` | Download the latest official release binary for macOS arm64 and replace the local `fd` binary in `~/bin`. |
| `fzf` | Fuzzy finder for shell history, files, and interactive selection. | `fzf --help` | Download the latest official release binary for macOS arm64 and replace the local `fzf` binary in `~/bin`. |
| `gh` | GitHub CLI for repositories, issues, pull requests, and auth. | `gh --help` | Download the latest official release binary for macOS arm64 and replace the local `gh` binary in `~/bin`. |
| `jq` | Command-line JSON parser and transformer. | `jq --help` | Download the latest official release binary for macOS arm64 and replace the local `jq` binary in `~/bin`. |
| `just` | Command runner for project-specific task recipes. | `just --help` | Download the latest official release binary for macOS arm64 and replace the local `just` binary in `~/bin`. |
| `rg` | Fast recursive text search tool from ripgrep. | `rg --help` | Download the latest official release binary for macOS arm64 and replace the local `rg` binary in `~/bin`. |
| `uv` | Python package and project manager with fast virtualenv and tool workflows. | `uv --help` | Download the latest official release binary for macOS arm64 and replace the local `uv` binary in `~/bin`. |
| `xh` | Friendly HTTP client for APIs and web services. | `xh --help` | Download the latest official release binary for macOS arm64 and replace the local `xh` binary in `~/bin`. |
| `yq` | Command-line YAML, JSON, and structured data processor. | `yq --help` | Download the latest official release binary for macOS arm64 and replace the local `yq` binary in `~/bin`. |
