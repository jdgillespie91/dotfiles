# ~/.codex/AGENTS.md

## User Preferences
- Prefer direct binary installations over Homebrew.
- Do not suggest Homebrew-based setup steps unless explicitly requested.
- When proposing installation instructions, prefer official release binaries and OS/arch-aware install steps.

## Tool Inventory
- Read `~/.codex/TOOLS.md` before checking whether common CLI tools are installed.
- Use `~/.codex/TOOLS.md` as the source of truth for available tools, help commands, and update conventions.
- When installing a new CLI tool, update `~/.codex/TOOLS.md`.
